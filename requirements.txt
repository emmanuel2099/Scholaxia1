fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
redis==5.0.4
celery==5.4.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
python-multipart==0.0.9
httpx==0.27.0

# Cloudinary (Media Storage)
cloudinary==1.40.0

# Stripe (Payments)
stripe>=9.5.0

# Firebase (Push Notifications)
firebase-admin==6.5.0

# Groq (Sia AI — free cloud API)
groq==0.9.0

# ElevenLabs (Sia Voice — Text-to-Speech, optional premium)
elevenlabs==1.2.0

# Free neural TTS fallback (works without API key — used when ElevenLabs is unset)
edge-tts==6.1.19

# gTTS fallback TTS
gTTS==2.5.1

# WebSockets
websockets==12.0
python-socketio==5.11.2

# Utilities
Pillow>=10.4.0
aiofiles==23.2.1

# PDF text extraction (admin CBT PDF import)
pypdf==4.2.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
livekit-api>=1.0.0
