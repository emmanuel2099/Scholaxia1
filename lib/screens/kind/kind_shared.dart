import 'package:flutter/material.dart';

/// Kid app brand colors (accent stays the same in light/dark).
class KidColors {
  static const accent = Color(0xFF6C63FF);
  static const learn = Color(0xFF00C896);
  static const quiz = Color(0xFFFFB347);
}
