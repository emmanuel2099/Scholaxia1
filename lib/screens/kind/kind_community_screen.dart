import 'package:flutter/material.dart';

import '../student/community/community_screen.dart';

/// Kid learners — same community as students, kid-safe shell.
class KindCommunityScreen extends StatelessWidget {
  const KindCommunityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const CommunityScreen();
  }
}
