import 'package:flutter/material.dart';

import '../classes/class_packages_screen.dart';

/// Backward-compatible route used by existing Subscribe buttons.
class SubscriptionScreen extends StatelessWidget {
  const SubscriptionScreen({super.key});

  @override
  Widget build(BuildContext context) => const ClassPackagesScreen();
}
